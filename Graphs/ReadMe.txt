這裡要寫一下，

怎麼練習這裡的code，

Graph要練的就三個，Graph的實作，DFS BFS



Graph要實作出vertex 跟Graph這二個class

DFS的話 ，一個是 connected componet, 這有二個實作方式 iter , Recursive
         接下來是用 yield 去回傳所有可能的path出來.
         
BFS  ,   connected componet只有一種實作方式，
         path也只有一種 ，
         但最後，有多寫一個，只輸出最近的path.
         
        
 
DFS, BFS 的connected componet的實作，很簡單，都是相似的pattern，

 差別只是在存vertex用的資結，一個是stack，一個是queue.
 
 
再多看一次，發現在找path方面，這二個演算法的樣式很像， 

差別也是一個， 是存的資結不同。

